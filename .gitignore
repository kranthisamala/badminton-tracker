node_modules/
*.log
.firebase/
firebase-debug.log
firestore-debug.log

# Firebase Admin SDK service account key(s) -- grant full project access,
# never commit. See scripts/provision-member-accounts.js.
serviceAccountKey*.json
*-firebase-adminsdk-*.json

# Timestamped Firestore snapshots written by the migration/backfill scripts.
backup-trackerData-*.json
backup-roles-*.json

# Local-only credentials (test account passwords etc). Never commit.
*.local.json
